/**
 * Placeholder: returns whether the current user is authenticated.
 * Wire to auth context / session when auth is implemented.
 */
export function useIsAuthenticated(): boolean {
  // TODO: read from AuthContext / session / cookie
  return false
}
