/**
 * App config and env-driven constants.
 * Add values here as needed (e.g. API base URL, feature flags).
 */
export const config = {
  // apiBaseUrl: import.meta.env.VITE_API_URL ?? '',
} as const
